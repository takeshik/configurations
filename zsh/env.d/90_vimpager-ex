# -*- mode: sh; encoding: utf-8; tab-width: 4; indent-tabs-mode: nil; -*-

if where vimpager-ex > /dev/null; then
    export PAGER==vimpager-ex
fi

if where vimmanpager-ex > /dev/null; then
    export MANPAGER==vimmanpager-ex
fi

# vim:set ft=sh fenc=utf-8 ts=4 sw=4 sts=4 et:
